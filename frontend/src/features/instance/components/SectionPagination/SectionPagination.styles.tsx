import styled from "@emotion/styled";

export const SectionsButtonsContainer = styled("div")({
  display: "flex",
  justifyContent: "space-between",
});

export const LeftSideButtonsContainer = styled("div")({
  display: "flex",
  gap: "2rem",
});
